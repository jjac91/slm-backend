const { test } = require("./apitest");

describe("geocode calls", function () {
    test("works for same user", async function () {
        const resp = test
          console.log(resp)
        expect.anything();
      });
});

